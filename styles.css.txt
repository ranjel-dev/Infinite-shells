:root{
  --boardW: 760px;
  --boardH: 360px;
  --shellSize: 120px;
  --pearlSize: 64px;
}

*{ box-sizing:border-box; }

body{
  font-family: system-ui, -apple-system, Segoe UI, Roboto, Arial, sans-serif;
  margin:0; padding:24px;
  background:#0b0b10; color:#e8e8ef;
}

#hud{ max-width:var(--boardW); margin:0 auto 14px; display:grid; gap:10px; }
#scoreLine{ font-size:18px; }
#msg{ opacity:.92; }

#controls button{
  margin-right:10px; padding:10px 14px;
  border-radius:10px; border:1px solid #2c2c3a;
  background:#151522; color:#e8e8ef; cursor:pointer;
}
#controls button:hover{ filter:brightness(1.1); }

#board{
  width:var(--boardW); height:var(--boardH);
  margin:0 auto;
  border-radius:22px; border:1px solid #2c2c3a;
  position:relative; overflow:hidden;
  background:#0F2F1F;
}

#board.shake{ animation: shake 320ms ease; }
@keyframes shake{
  0%{transform:translate(0,0)}
  20%{transform:translate(-6px,0)}
  40%{transform:translate(6px,0)}
  60%{transform:translate(-5px,0)}
  80%{transform:translate(5px,0)}
  100%{transform:translate(0,0)}
}

#overlay{
  position:absolute; inset:0;
  background:rgba(255,40,60,.22);
  opacity:0; pointer-events:none;
  transition:opacity 160ms ease;
}
#overlay.flash{ opacity:1; }

#recordText{
  position:absolute; top:18px; left:50%;
  transform:translateX(-50%);
  font-weight:800; letter-spacing:1px;
  color:#ffd54a;
  opacity:0; transition:opacity .25s ease;
}
#recordText.show{ opacity:1; }

#shellLayer{ position:absolute; inset:0; }

.shell{
  position:absolute;
  width:var(--shellSize); height:var(--shellSize);
  bottom:92px;
  left:50%;
  transform:translateX(-50%);
  cursor:pointer;
  background-size:contain;
  background-repeat:no-repeat;
  background-position:center;
  transition:left 220ms ease, transform 220ms ease;
  filter:drop-shadow(0 10px 12px rgba(0,0,0,.35));
}

.shell.lift{
  transform:translateX(-50%) translateY(-22px) scale(1.03);
}

#pearl{
  position:absolute;
  width:var(--pearlSize); height:var(--pearlSize);
  bottom:112px;
  left:50%;
  transform:translateX(-50%);
  opacity:0;
  transition:opacity 140ms ease;
  background-size:contain;
  background-repeat:no-repeat;
  background-position:center;
  filter:drop-shadow(0 8px 10px rgba(0,0,0,.35));
}